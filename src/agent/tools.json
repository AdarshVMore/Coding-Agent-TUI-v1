{
  "availableTools": [
    {
      "name": "exec",
      "description": "Execute terminal commands",
      "parameters": {
        "command": "string"
      }
    },
    {
      "name": "web_search",
      "description": "Search the web for external information",
      "parameters": {
        "query": "string"
      }
    },
    {
      "name": "edit",
      "description": "Edit a file",
      "parameters": {
        "filePath": "string",
        "edits": "string"
      }
    }
  ],
  "instructions": {
    "toolUsage": "Only call tools when necessary. Prefer reading files before editing them.",
    "responseFormat": {
      "toolName": "string",
      "inputs": "object",
      "reasoning": "string"
    }
  }
}
